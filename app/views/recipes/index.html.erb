<h1>Muncher</h1>

<section class="search_form">
  <%= render "form" %>
</section>

<h2>Results for <%= @query %></h2>

<% if @recipes %>
  <ul>
    <% @recipes.each do |recipe| %>
    <li>
      <%= recipe.title %>
    </li>
    <% end %>
  </ul>
<% end %>
